<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/tertiary_60"/> <!-- Cambia este color al deseado -->
            <corners android:radius="24dp"/> <!-- Ajusta el radio si es necesario -->
        </shape>
    </item>
    <item android:drawable="@android:color/transparent"/> <!-- Estado por defecto -->
</selector>
